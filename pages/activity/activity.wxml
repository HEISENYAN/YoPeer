
<scroll-view class = "main-page" type = "custom" scroll-y="true">
  <!--顶部状态栏-->
  <!-- <view class = "top-margin" style = "height:{{statusBarHeight}}px"> </view> -->
  <view style = "position: relative; width:100%;height:42vh;">
    <view class = "navigation-bar">welcome to YoPeer</view>
    <view class = "notice-bar"></view>
  </view>
  <view class = "swiper-container">
    <t-swiper 
      current="{{1}}"
      autoplay="{{true}}"
      duration="{{500}}"
      interval="{{5000}}"
      list="{{swiperList}}"
      height = "100rpx"
      navigation="{{ { type: 'dots-bar' } }}"
      class = "notice-swiper"
    />
  </view>
  
  <view class = "activity-cell" bindtap = "onSelectActivity"></view>
  <view class="subTitle">Weekend x 周末活动</view>
  <view class = "activity-cell" bindtap = "onSelectActivity"></view>
  <view class = "activity-cell" bindtap = "onSelectActivity"></view>

</scroll-view>